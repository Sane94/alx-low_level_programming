#include "main.h"
#include <stdio.h>

/**
 * _isupper - check for uppercase
 *
 * @c: ASCII code
 * Return: Always 1 (success)
 * */
int _isupper(int c)
{
if (c => 97 && c =< 122)
{
return (1);
}
else
{
reture (0);
}
_putchar(/n);
}
