#!/bin/bash
#include <stdio.h>
#include <string.h>
/**
 * main - Entry point
 * Return: Always 0 (successfully)
 * Sanae Valet
 */
int main (void)
{
	puts("/"Programming is like building a multiligual puzzle");
	return (0);
}
